# Finance Research App

Streamlit dashboard for processing pasted article text or uploaded `.txt` and `.md` files with the local rule-based article reader.

## Run

```bash
streamlit run apps/finance_research_app/app.py
```

The app saves markdown analysis files to `research/processed/`.
